---
# Type of node manager or worker
swarm_type_of_node: "manager"
# IP address of the swarm leader
# swarm_leader_address: "{{ ansible_host == swarm_leader_address }}"
# swarm leader port
swarm_leader_port: 2377
